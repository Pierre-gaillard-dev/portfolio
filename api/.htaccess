RewriteEngine On

# Autoriser l'accès direct aux fichiers existants (comme adminer.php, .css, .js, etc.)

RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Rediriger tout le reste vers index.php

RewriteRule ^ index.php [QSA,L]
